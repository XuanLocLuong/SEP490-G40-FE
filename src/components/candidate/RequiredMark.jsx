/** Dấu * đỏ cho field bắt buộc apply trên hồ sơ Candidate. */
const RequiredMark = () => (
    <span className="cp-required" title="Bắt buộc để ứng tuyển" aria-label="bắt buộc">
        *
    </span>
);

export default RequiredMark;
