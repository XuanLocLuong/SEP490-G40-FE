/** Dấu * cho label trường bắt buộc — dùng chung toàn app */
const RequiredMark = () => (
    <span className="required-mark" aria-hidden="true">
        {' '}
        *
    </span>
);

export default RequiredMark;
