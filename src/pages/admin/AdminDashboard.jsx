const AdminDashboard = () => {
    return (
        <div>
            <h1>Admin Dashboard</h1>
            <p>Placeholder — platform operations, users, moderation, system health code ở task riêng.</p>
        </div>
    );
};

export default AdminDashboard;
