const RecruiterHomePage = () => {
    return (
        <div>
            <h1>Recruiter Homepage</h1>
            <p>Placeholder — nội dung thật (jobs, applicants, recommendations...) code ở task riêng.</p>
        </div>
    );
};

export default RecruiterHomePage;
