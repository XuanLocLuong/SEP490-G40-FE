/** Số card mỗi trang trên grid recruiter (≈ 3 cột × 3 hàng). */
export const RECRUITMENT_CARD_GRID_PAGE_SIZE = 9;

/** Tab mức phù hợp mặc định khi xem ứng viên Chờ duyệt. */
export const DEFAULT_MATCH_BUCKET_KEY = 'highMatch';
